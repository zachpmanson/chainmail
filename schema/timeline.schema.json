{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/zachpmanson/chainmail/schema/timeline.schema.json",
  "title": "Timeline",
  "description": "One email trail, unspooled out of its threads and forwards into a single chronological transcript. Produced by a collector (e.g. the mail-timeline skill); consumed by the chainmail renderer. The fields that carry HTML (body, subtitle, openItems) are rendered as authored, so a spec from an untrusted source must be sanitised before it is rendered.",
  "type": "object",
  "required": ["title", "messages"],
  "additionalProperties": false,
  "properties": {
    "specVersion": {
      "type": "integer",
      "description": "Contract version. Absent means 1."
    },
    "title": {
      "type": "string",
      "description": "Page title. A leading # renders as a channel-style hash."
    },
    "subtitle": { "type": "string", "description": "One or two sentences. Provenance belongs in queries/threads/sourceNotes, not here. Rendered as authored (inline <em>, <b>, <code>, <a> with an http(s) or #fragment href); a spec from an untrusted source must be sanitised before rendering." },
    "runLabel": { "type": "string", "description": "Names this collection pass, e.g. 'pass 2, 20 Aug 2026'." },
    "runs": { "type": "array", "items": { "type": "string" }, "description": "Labels of every pass so far, oldest first." },
    "theme": {
      "enum": ["light", "dark", "auto"],
      "description": "Renderer theme. Omitted means the renderer's default (light)."
    },
    "openItemsTitle": {
      "type": "string",
      "description": "Heading over the unresolved-questions section. Omitted means 'Still open'."
    },
    "openItems": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Unresolved questions, commitments and ownerless decisions. HTML from the subtitle allowlist: inline <em>, <b>, <code>, <a> with an http(s) or #fragment href. A spec from an untrusted source must be sanitised before rendering."
    },
    "avatars": {
      "type": "object",
      "description": "Sender name -> image. Only a data: image URI or an http(s) URL is rendered — the renderer refuses anything else. Keys must match Message.sender exactly.",
      "additionalProperties": { "type": "string" }
    },
    "participants": {
      "type": "array",
      "description": "The full cast, including people who only ever appear as recipients.",
      "items": {
        "type": "object",
        "required": ["name"],
        "additionalProperties": false,
        "properties": {
          "name": { "type": "string" },
          "org": { "type": "string" },
          "email": { "type": "string", "description": "Omit when no address appears anywhere in the trail. Never guess one." },
          "role": { "type": "string" },
          "note": { "type": "string", "description": "Use instead of role for cc-only people and aliases." }
        }
      }
    },
    "runParams": {
   "type": "object",
   "description": "Selection parameters this run was invoked with, so a later refresh reproduces the same page. Only what the rest of the spec cannot already supply; never a machine-local path.",
   "additionalProperties": false,
   "properties": {
    "me": {
     "type": "array",
     "items": { "type": "string" },
     "description": "Addresses belonging to the reader, whose messages render as outbound."
    },
    "limit": { "type": "integer", "description": "How many chains the search was allowed to select." },
    "person": { "type": "string", "description": "Address, name or slack uid the selection was filtered to." },
    "since": { "type": "string", "description": "YYYY-MM-DD floor on entry dates." }
   }
  },
  "queries": {
      "type": "array",
      "description": "Searches run, so a null result is interpretable.",
      "items": {
        "oneOf": [
          { "type": "string" },
          {
            "type": "object",
            "required": ["q"],
            "additionalProperties": false,
            "properties": { "q": { "type": "string" }, "note": { "type": "string" } }
          }
        ]
      }
    },
    "threads": {
      "type": "array",
      "description": "Mail threads the transcript was assembled from.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "subject": { "type": "string" },
          "id": { "type": "string", "description": "Gmail thread or message id; links out." },
          "count": { "type": "integer" },
          "span": { "type": "string" },
          "note": { "type": "string" }
        }
      }
    },
    "sourceNotes": {
      "type": "array",
      "description": "Provenance caveats: avatar sources, unrecoverable addresses, truncation.",
      "items": {
        "type": "object",
        "required": ["title", "items"],
        "additionalProperties": false,
        "properties": {
          "title": { "type": "string" },
          "items": { "type": "array", "items": { "type": "string" } }
        }
      }
    },
    "messages": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/definitions/Entry" }
    }
  },
  "definitions": {
    "Entry": {
      "type": "object",
      "required": ["date", "body"],
      "additionalProperties": false,
      "properties": {
        "kind": { "enum": ["message", "note"], "description": "'note' is a meeting or event that never existed as an email. Omitted means message." },
        "id": { "type": "string", "description": "Stable anchor. Omit to derive from date+time+sender; content-derived ids survive inserting entries mid-trail." },
        "date": { "type": "string", "description": "As displayed, e.g. 'Thu 16 Jul 2026'. Not normalised." },
        "time": { "type": "string", "description": "As displayed, e.g. '11:35'. Absent for undated notes." },
        "tz": { "type": "string", "description": "The zone this entry's clock is in, e.g. 'NZST', '+0530'. Drives absolute-time ordering. Omit rather than guess: absent means the zone is unknown and the renderer shows it as unknown." },
        "tzSource": { "enum": ["stated", "inferred"], "description": "Where tz came from. 'stated' is the source's own Date header or zone label. 'inferred' was worked out from other evidence and must be presented as a claim, not a fact. Omit when tz is absent." },
        "sender": { "type": "string" },
        "org": { "type": "string", "description": "Drives colour, assigned in first-appearance order." },
        "fromEmail": { "type": "string" },
        "to": { "type": "string", "description": "Free text as it appeared, including 'cc ...'." },
        "subject": { "type": "string", "description": "Names the chain when this entry opens one. Capture it during collection." },
        "label": { "type": "string", "description": "Notes only: what the meeting was." },
        "body": { "type": "string", "description": "Rendered HTML, produced through the collector's allowlist (internal/spec/sanitise.go): blocks <p>, <div>, <blockquote>, <ul>/<ol>/<li>, <table>/<tr>/<td>/<th>, <pre>, <h1>-<h6>, <hr>; inline <a href> (http(s) only), <img src> (http(s) or data:image only), <b>, <i>, <em>, <strong>, <u>, <s>, <code>, <span style>; the editorial gloss (<p class=\"ed\">) and the signature fold (<details class=\"sig\">, <summary title>, <div class=\"sigbd\">). Nothing else — no scripts, stylesheets, event handlers or other URL schemes. The collector enforces this; a spec that did not come from the collector is only as safe as its author, so sanitise an untrusted spec before rendering." },
        "quoted": { "type": "boolean", "description": "Reconstructed from quoted text; never existed as a standalone message here." },
        "me": { "type": "boolean", "description": "The reader's own outbound." },
        "source": { "type": "string", "description": "Where it came from: a Gmail id, or 'unspooled from the 2 Aug email'." },
        "gmailId": { "type": "string" },
        "threadId": { "type": "string" },
        "parent": { "type": "string", "description": "Id of the entry this replies to. Absent means it opens a chain. Coverage here drives ordering, lanes and the reply tree." },
        "edits": {
          "type": "array",
          "description": "A quoter's in-place change to a message it quoted (issue #42). Each names the base that was edited, who made the change (the quoter), when, and the quoter's modified text. The renderer draws these inline inside this message instead of floating the derived copy as its own node.",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "id": { "type": "string", "description": "Spec id of the derived (edited) copy; the diff source." },
              "base": { "type": "string", "description": "Spec id of the original message the change was made to." },
              "who": { "type": "string", "description": "The quoter who made the edit." },
              "time": { "type": "string", "description": "When the quoting message was sent, e.g. '14:00'." },
              "body": { "type": "string", "description": "The quoter's modified text of the quote, as stored (plain text, not HTML)." }
            }
          }
        },
        "meta": { "type": "boolean", "description": "Meeting scheduling exhaust (invitation, notes, accept/decline). Forces the classification the renderer would otherwise infer." },
        "mentions": { "type": "array", "items": { "type": "string" } },
        "attachments": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["name"],
            "additionalProperties": false,
            "properties": {
              "name": { "type": "string" },
              "kind": { "type": "string" },
              "size": { "type": "string" },
              "gmailId": { "type": "string", "description": "Links to the message in Gmail. Only ever present on real messages." },
              "link": { "type": "string", "description": "Opens the attachment at its source, for attachments not reached through Gmail." },
              "preview": { "type": "string", "description": "Thumbnail as a data: URI. Never a URL — the page renders without a network.", "pattern": "^data:image/" },
              "previewW": { "type": "integer", "minimum": 1 },
              "previewH": { "type": "integer", "minimum": 1 }
            }
          }
        }
      }
    }
  }
}
